type Address {
  street: String
  suite: String
}

type User {
  id: ID!
  name: String
  username: String
  email: String
  address: Address
}

type Query {
  users: [User]
  user(userId: ID!): User
}

schema {
  query: Query
}
